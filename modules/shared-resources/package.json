{
  "name": "@jrapp/shared-resources",
  "version": "1.0.0",
  "description": "Shared resources between client and server",
  "author": "Jon Rapp",
  "license": "ISC",
  "main": "dist/index.js",
  "scripts": {
    "test:server": "mocha --opts test/mocha.opts -r ts-node/register ./src/**/*.test.ts",
    "build": "rimraf ./dist && tsc -p ./",
    "dev": "rimraf ./dist && tsc -w -p ./",
    "start": "node ./dist/index.js",
    "prod": "npm run build && npm run start"
  },
  "dependencies": {
    "underscore": "^1.10.2"
  },
  "devDependencies": {
    "@types/chai": "4.0.3",
    "@types/mocha": "^5.2.7",
    "@types/node": "^6.0.101",
    "chai": "^4.1.1",
    "eslint": "^4.4.1",
    "mocha": "^6.2.0",
    "ts-node": "^3.3.0",
    "typescript": "^3.9.6",
    "rimraf": "^3.0.2"
  },
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  }
}
