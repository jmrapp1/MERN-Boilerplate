
export default abstract class Resource {

    /**
     * Represents whether or not the resource's data was validated through the mapper constraints
     * @type {boolean}
     */
    validated = false;

}
